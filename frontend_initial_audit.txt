Frontend initial audit
======================

Fecha: 2026-06-20
Repositorio: C:\Users\juany\Desktop\ev3_nanhes
Rama actual: feature/web-frontend
API base esperada: http://localhost:8000

1. Existe la carpeta web/?
No. En la raiz del repo no existe web/.

2. Ya hay un proyecto Next.js dentro de web/?
No. Como web/ no existe, tampoco hay proyecto Next.js creado ahi.

3. Esta instalado Tailwind?
No se encontro instalacion ni configuracion frontend:
- No existe web/package.json.
- No existe package.json en el repo.
- No existe tailwind.config.*.
- No existe postcss.config.*.

4. Archivos principales existentes
- package.json: no existe.
- app/: no existe.
- pages/: no existe.
- src/: existe en la raiz, pero corresponde al proyecto Python/Kedro: src/ev3_nhanes/.
- components/: no existe.
- tailwind.config: no existe.
- .env.local o .env.example: no existen.

Archivos/carpetas relevantes detectados en la raiz:
- PLAN.md
- README.md
- feature_schema.json
- api/
- src/
- tests/
- conf/
- data/
- notebooks/
- pyproject.toml
- requirements.txt
- uv.lock

5. Existe algun cliente API ya creado?
No se encontro cliente API frontend. No hay carpeta web/, no hay package.json, y no hay archivos tipicos como api client, lib/api, axios/fetch wrapper, etc.

6. Existe alguna pagina inicial?
No. No existe web/app/page.*, web/pages/index.*, ni ninguna estructura Next.js.

7. Que habria que crear o modificar para cumplir esta tarea?
Crear el frontend desde cero dentro de web/:
- Inicializar un proyecto Next.js con React.
- Instalar/configurar Tailwind.
- Crear configuracion de entorno para la API, por ejemplo NEXT_PUBLIC_API_BASE_URL=http://localhost:8000.
- Crear cliente API para consumir:
  - GET /schema
  - POST /predict
  - POST /explain
  - GET /metrics, si aplica al MVP
- Crear pagina inicial del MVP.
- Renderizar el formulario desde feature_schema.json via GET /schema.
- Mostrar resultados de /predict: es_longevo, probabilidad, edad_biologica y gap si hay edad_cronologica.
- Integrar visualizacion de SHAP desde /explain si esta dentro del alcance de esta tarea.
- Asegurar CORS contra http://localhost:3000. El backend ya permite http://localhost:3000 y http://127.0.0.1:3000 en api/main.py.

8. Que comandos debo usar para correr el frontend?
Todavia no hay comandos frontend porque web/ no existe.

Cuando se cree con Next.js, el flujo esperado sera algo como:
cd web
npm install
npm run dev

La app deberia quedar disponible en:
http://localhost:3000

Antes o en paralelo, levantar la API desde la raiz del repo:
pip install -r api/requirements.txt
uvicorn api.main:app --reload

API docs:
http://localhost:8000/docs

9. Hay riesgos de romper algo existente?
Riesgo bajo si el trabajo se limita a crear web/ y no se modifican archivos existentes.

Riesgos a considerar:
- No tocar src/, api/, feature_schema.json ni configuraciones Kedro salvo que sea estrictamente necesario.
- Evitar cambios en requirements.txt/pyproject.toml para dependencias frontend; el frontend deberia manejar sus dependencias dentro de web/package.json.
- Si se agrega docker-compose o configuracion compartida mas adelante, puede impactar el flujo local de backend.
- El contrato del form depende de GET /schema y feature_schema.json; cambios en ese contrato pueden romper backend/tests.

Conclusion:
La rama feature/web-frontend esta lista para iniciar el frontend desde cero. No hay implementacion Next.js/Tailwind existente que preservar dentro de web/.
